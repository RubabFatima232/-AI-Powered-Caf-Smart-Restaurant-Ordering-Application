<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="200dp"
    android:layout_marginVertical="8dp"
    app:cardCornerRadius="20dp"
    app:cardElevation="6dp"
    app:cardBackgroundColor="?attr/colorSurface">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- auto-sliding image carousel -->
        <androidx.viewpager2.widget.ViewPager2
            android:id="@+id/pagerImages"
            android:layout_width="0dp"
            android:layout_height="0dp"
            app:layout_constraintBottom_toTopOf="@id/guideline"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <androidx.constraintlayout.widget.Guideline
            android:id="@+id/guideline"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            app:layout_constraintGuide_percent="0.55" />

        <!-- AI confidence chip -->
        <com.google.android.material.chip.Chip
            android:id="@+id/chipConfidence"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="12dp"
            android:text="98 % match"
            android:textColor="@android:color/white"
            app:chipIcon="@drawable/ic_ai_chip"
            app:chipBackgroundColor="?attr/colorPrimary"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- textual info -->
        <TextView
            android:id="@+id/tvFeaturedName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginTop="8dp"
            android:text="Dish Name"
            android:textAppearance="?attr/textAppearanceHeadline6"
            android:textColor="?attr/colorOnSurface"
            app:layout_constraintEnd_toStartOf="@id/btnAddFeatured"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/guideline" />

        <TextView
            android:id="@+id/tvFeaturedDesc"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Chef’s special with AI twist"
            android:textAppearance="?attr/textAppearanceBody2"
            android:textColor="?attr/colorOnSurfaceVariant"
            app:layout_constraintEnd_toEndOf="@id/tvFeaturedName"
            app:layout_constraintStart_toStartOf="@id/tvFeaturedName"
            app:layout_constraintTop_toBottomOf="@id/tvFeaturedName" />

        <TextView
            android:id="@+id/tvFeaturedPrice"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="$0"
            android:textAppearance="?attr/textAppearanceHeadline5"
            android:textColor="?attr/colorPrimary"
            app:layout_constraintStart_toStartOf="@id/tvFeaturedName"
            app:layout_constraintTop_toBottomOf="@id/tvFeaturedDesc" />

        <!-- nutrition swipe hint -->
        <ImageButton
            android:id="@+id/btnNutrition"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginStart="8dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_info"
            android:tint="?attr/colorPrimary"
            app:layout_constraintBottom_toBottomOf="@id/tvFeaturedPrice"
            app:layout_constraintStart_toEndOf="@id/tvFeaturedPrice"
            app:layout_constraintTop_toTopOf="@id/tvFeaturedPrice" />

        <!-- add button with lottie inside -->
        <FrameLayout
            android:id="@+id/btnAddFeatured"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_marginEnd="16dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <ImageView
                android:id="@+id/imgPlus"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:padding="8dp"
                android:src="@drawable/ic_add_circle_24"
                android:tint="?attr/colorPrimary" />

            <com.airbnb.lottie.LottieAnimationView
                android:id="@+id/lottieSparkle"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:visibility="invisible"
                app:lottie_rawRes="@raw/sparkle"
                app:lottie_autoPlay="false"
                app:lottie_loop="false" />
        </FrameLayout>
    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>